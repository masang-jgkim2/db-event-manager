# API 서버 주소 (미설정 시 http://localhost:4000/api 사용)
# 외부 PC에서 접속할 때는 서버 PC의 IP로 설정
# VITE_API_URL=http://192.168.0.10:4000/api

# 로그인 화면 admin / admin123 안내 블록 (기본 비활성 — 켜려면 아래 주석 해제)
# VITE_SHOW_LOGIN_DEFAULT_ACCOUNT_HINT=true
